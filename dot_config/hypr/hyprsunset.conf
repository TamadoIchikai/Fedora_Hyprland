# 4:30 AM - Turn OFF the blue light filter (Daytime mode)
profile {
    time = 04:30
    identity = true
}

# 5:30 PM - Turn ON the blue light filter (Night mode)
profile {
    time = 17:30
    temperature = 3500  # Adjust this value (e.g., 2500-4500). Lower is warmer/redder.
}
