####################################
########## file overrides #########
####################################

# Load user addons from inside the mpv `scripts/` folder so they are not
# wiped when mpv-file-browser is updated/reinstalled.
# mpv ignores scripts/ file-browser-addons/ (no main.lua), so only file-browser reads this.
addon_directory=~~/scripts/file-browser-addons